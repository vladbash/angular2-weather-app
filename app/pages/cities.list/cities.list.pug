.row
    .col-sm-4(*ngFor="let city of citiesList | deleted")
        city-card([city]="city", (deleteEvent)="deleteCity($event)")
    .col-sm-4
        city-card((updateEvent)="updateCityWeatherList()")